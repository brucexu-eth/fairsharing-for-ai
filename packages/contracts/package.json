{
  "name": "@fairsharing/contracts",
  "private": true,
  "scripts": {
    "build": "bun hardhat compile",
    "test": "bun hardhat test",
    "deploy:local": "bun hardhat run scripts/deploy.ts --network localhost",
    "deploy:base-sepolia": "bun hardhat run scripts/deploy.ts --network base-sepolia",
    "deploy:base": "bun hardhat run scripts/deploy.ts --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ignition": "^3.1.0",
    "@nomicfoundation/hardhat-ignition-ethers": "^3.1.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/chai": "^5.2.3",
    "chai": "^6.2.2",
    "hardhat": "^2.22.4",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
